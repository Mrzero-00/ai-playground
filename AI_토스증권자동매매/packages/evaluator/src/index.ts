import type{InvestmentThesis,TradeEvaluation,TradeRecord}from '@trading/shared';
export type EvaluationEvidence={thesisStillValid:boolean;entryWithinPlan:boolean;stopHonored:boolean;dataWasFresh:boolean;thesisScoreAfter:number};
export function evaluateTrade(t:TradeRecord,thesis:InvestmentThesis,e:EvaluationEvidence):TradeEvaluation{if(t.pnlAmount===null)throw new Error('Cannot evaluate an open trade');const profitable=t.pnlAmount>0;const executionCorrect=e.entryWithinPlan;const riskControlCorrect=e.stopHonored&&e.dataWasFresh;const thesisCorrect=e.thesisStillValid&&e.thesisScoreAfter>=7;let outcomeType:TradeEvaluation['outcomeType'];if(thesisCorrect&&executionCorrect)outcomeType=profitable?'good_thesis_good_execution':'good_process_bad_outcome';else if(thesisCorrect)outcomeType='good_thesis_bad_execution';else if(executionCorrect)outcomeType=profitable?'bad_thesis_lucky_profit':'bad_thesis_good_execution';else outcomeType=profitable?'bad_process_good_outcome':'good_thesis_bad_execution';return{tradeId:t.id,profitable,thesisCorrect,executionCorrect,riskControlCorrect,outcomeType,thesisScoreBefore:thesis.thesisScore,thesisScoreAfter:e.thesisScoreAfter,executionIssues:executionCorrect?[]:['entry_outside_plan'],thesisIssues:thesisCorrect?[]:['thesis_invalidated_or_weakened'],riskIssues:riskControlCorrect?[]:['risk_or_data_quality_violation'],lessons:[profitable&&!thesisCorrect?'Do not reinforce lucky profits':'Keep thesis and execution attribution separate']};}
