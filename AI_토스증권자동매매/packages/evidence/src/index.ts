import {EvidenceBundleSchema,type EvidenceBundle,type EvidenceRecord,type MarketCandidate} from '@investment-os/contracts';
export interface EvidenceProvider{get(symbol:string,asOf:string):Promise<EvidenceRecord[]>}
export class MockEvidenceProvider implements EvidenceProvider{private readonly records:EvidenceRecord[];constructor(records:EvidenceRecord[]){this.records=records;}async get(symbol:string,asOf:string){return structuredClone(this.records.filter(v=>(v.symbol===symbol||v.symbol===null)&&Date.parse(v.validFrom)<=Date.parse(asOf)));}}
export async function buildEvidenceBundle(candidate:MarketCandidate,provider:EvidenceProvider):Promise<EvidenceBundle>{const evidence=await provider.get(candidate.symbol,candidate.observedAt);return EvidenceBundleSchema.parse({symbol:candidate.symbol,asOf:candidate.observedAt,evidence,marketSnapshot:{price:candidate.price,changePercent:candidate.changePercent,relativeVolume:candidate.relativeVolume,dollarVolume:candidate.dollarVolume,spreadPercent:candidate.spreadPercent,premarketGapPercent:candidate.premarketGapPercent},fundamentals:{marketCap:candidate.marketCap,floatShares:candidate.floatShares},riskSignals:[]});}
