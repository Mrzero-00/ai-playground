import type {InvestmentThesis,PriceReflectionAssessment,SignalFeatures} from '@trading/shared';
export function assessPriceReflection(t:InvestmentThesis,f:SignalFeatures):PriceReflectionAssessment{const chase=Math.min(10,Math.max(0,f.premarketGapPercent/8+Math.max(0,f.vwapDistancePercent)/4));const dilution=Math.min(10,t.dilutionRisks.length*2.5);const liquidity=Math.min(10,f.spreadPercent*3);const unknown=t.unknowns.length>2||t.confidence<0.7;const overpriced=chase>=8||f.premarketGapPercent>60;const allowed=!unknown&&!overpriced&&dilution<7&&liquidity<7;return{symbol:t.symbol,reflectionLevel:unknown?'unknown':overpriced?'overpriced':f.premarketGapPercent<20?'underpriced':'fair',reflectedPercent:unknown?null:Math.min(100,Math.max(0,f.premarketGapPercent*1.5)),entryAllowed:allowed,reasons:[...(unknown?['thesis_uncertainty_too_high']:[]),...(overpriced?['chase_risk_too_high']:[]),...(dilution>=7?['dilution_pressure']:[]),...(liquidity>=7?['liquidity_risk']:[])],chaseRisk:chase,dilutionPressure:dilution,liquidityRisk:liquidity};}
