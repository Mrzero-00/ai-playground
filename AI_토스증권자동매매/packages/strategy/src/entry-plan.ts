import type {EntryPlan,PriceReflectionAssessment,SignalFeatures} from '@trading/shared';
export function createEntryPlan(f:SignalFeatures,r:PriceReflectionAssessment,currentPrice:number,now:string):EntryPlan{if(!r.entryAllowed)return{symbol:f.symbol,entryType:'no_entry',entryMin:null,entryMax:null,stopLoss:null,target1:null,target2:null,timeStopMinutes:null,maxHoldingMinutes:null,invalidationPrice:null,expiresAt:now,riskRewardTarget1:null,riskRewardTarget2:null};const volatility=Math.max(0.03,Math.min(0.08,Math.abs(f.intradayChangePercent)/200));const stop=currentPrice*(1-volatility);const risk=currentPrice-stop;return{symbol:f.symbol,entryType:f.vwapDistancePercent>2?'limit_pullback':'vwap_reclaim',entryMin:currentPrice*0.99,entryMax:currentPrice*1.005,stopLoss:stop,target1:currentPrice+risk*2,target2:currentPrice+risk*3.5,timeStopMinutes:30,maxHoldingMinutes:180,invalidationPrice:stop,expiresAt:new Date(Date.parse(now)+15*60_000).toISOString(),riskRewardTarget1:2,riskRewardTarget2:3.5};}
