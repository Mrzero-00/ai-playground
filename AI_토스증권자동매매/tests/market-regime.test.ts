import{describe,expect,it}from'vitest';import{assessMarketRegime}from'../packages/market-regime/src/index.js';
const now='2026-01-02T14:30:00.000Z';const quality={observedAt:now,receivedAt:now,source:'mock',sourceUrl:null,verified:true,staleAfterSeconds:30};const base={vix:18,indexAbove200Day:true,advanceDeclineRatio:1.4,marketDollarVolumeRatio:1,gapHoldRate:.7,quality};
describe('market regime fail-safe',()=>{it('allows full risk only in verified risk-on conditions',()=>expect(assessMarketRegime(base,now)).toMatchObject({regime:'risk_on',riskMultiplier:1,tradingAllowed:true}));it('blocks stale market data',()=>expect(assessMarketRegime(base,'2026-01-02T14:31:00.000Z')).toMatchObject({regime:'unknown',riskMultiplier:0,tradingAllowed:false}));it('blocks panic conditions',()=>expect(assessMarketRegime({...base,vix:45},now)).toMatchObject({regime:'panic',tradingAllowed:false}));});
