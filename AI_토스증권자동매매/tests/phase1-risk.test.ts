import{describe,expect,it}from'vitest';import{evaluateRisk}from'../packages/risk-gate/src/index.js';
const candidate={symbol:'ACME',observedAt:'2026-01-01T00:00:00.000Z',price:100,changePercent:5,relativeVolume:4,dollarVolume:10_000_000,marketCap:null,floatShares:null,spreadPercent:.2,premarketGapPercent:null,discoveryReasons:['volume']};const committee={bullScore:8,bearScore:2,marketScore:8,evidenceQuality:8,agreementScore:8,uncertaintyScore:2,unresolvedConflicts:[]};const plan={symbol:'ACME',entryType:'limit_pullback' as const,entryMin:99,entryMax:101,stopLoss:97,target1:108,target2:114,timeStopMinutes:180,expiresAt:'2026-01-01T00:15:00.000Z'};const portfolio={equityCents:10_000_000,dailyPnlCents:0,weeklyPnlCents:0,openPositions:0,consecutiveLosses:0,minutesSinceLastLoss:999,brokerConsistent:true,dataFresh:true};
describe('Phase 1 Risk Gate',()=>{it('cannot approve without a primary source',()=>{const result=evaluateRisk(candidate,{symbol:'ACME',asOf:candidate.observedAt,evidence:[],marketSnapshot:{},fundamentals:{},riskSignals:[]},committee,plan,portfolio);expect(result.approved).toBe(false);expect(result.rejectionReasons).toContain('official_or_primary_source_required');});it('blocks the daily loss limit',()=>{const result=evaluateRisk(candidate,{symbol:'ACME',asOf:candidate.observedAt,evidence:[{id:'e',symbol:'ACME',sourceType:'sec',sourceTier:1 as const,sourceUrl:null,publishedAt:candidate.observedAt,receivedAt:candidate.observedAt,validFrom:candidate.observedAt,rawContentRef:'x',contentHash:'12345678',sourceReliability:1}],marketSnapshot:{},fundamentals:{},riskSignals:[]},committee,plan,{...portfolio,dailyPnlCents:-100_000});expect(result.approved).toBe(false);expect(result.rejectionReasons).toContain('daily_loss_limit');});});
