import { describe, expect, it } from 'vitest'; import { RiskEngine } from '../packages/risk/src/index.js';
const base={equity:100_000,dailyPnl:0,weeklyPnl:0,consecutiveLosses:0,minutesSinceLastLoss:999,openPositions:0,entryPrice:10,stopPrice:9.5,dollarVolume:6_000_000,spreadPercent:0.5,gapPercent:20,killSwitchActive:false,regimeRiskMultiplier:1,dataFresh:true,estimatedRoundTripCostPercent:0.65,expectedGrossEdgePercent:4};describe('RiskEngine',()=>{it('sizes a valid position from fixed risk budget',()=>{const d=new RiskEngine().evaluate(base);expect(d.approved).toBe(true);expect(d.quantity).toBe(500);expect(d.riskAmount).toBe(250);});it('always respects the kill switch',()=>{const d=new RiskEngine().evaluate({...base,killSwitchActive:true});expect(d.approved).toBe(false);expect(d.quantity).toBe(0);});it('blocks stale data and an edge below costs',()=>{const d=new RiskEngine().evaluate({...base,dataFresh:false,expectedGrossEdgePercent:0.5});expect(d.reasons).toContain('stale_or_unverified_data');expect(d.reasons).toContain('expected_edge_does_not_cover_costs');});});
